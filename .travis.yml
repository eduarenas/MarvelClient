# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
script:
- set -o pipefail && xcodebuild test -workspace Example/MarvelClient.xcworkspace -scheme MarvelClient -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
